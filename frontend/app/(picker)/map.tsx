import { LayoutMapScreen } from '@/components/layout-map-screen';

export default function PickerMapScreen() {
  return (
    <LayoutMapScreen
      tone="picker"
      title="Floor map"
      subtitle="Follow the route — shelves turn green when you confirm a pick"
    />
  );
}
