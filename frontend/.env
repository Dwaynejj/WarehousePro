# Supabase — fill these in from your project dashboard:
#   Project Settings -> API
#
# EXPO_PUBLIC_* vars are inlined into the JS bundle at build time, so treat them
# as PUBLIC. Only the publishable/anon key belongs here — never the service_role
# key, which bypasses Row Level Security.
#
# Metro caches inlined values: restart with `npx expo start -c` after editing.

EXPO_PUBLIC_SUPABASE_URL=https://aboijzdngtaudcqxjmss.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_0Rl-8AcPAH_HTTu3LaONQg_bMOh22Ry
EXPO_PUBLIC_API_BASE_URL=http://172.20.10.2:8080